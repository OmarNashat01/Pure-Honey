server {
        listen 80 default_server;
        listen [::]:80 default_server;


        root /var/www/html;

        index index.html index.htm index.nginx-debian.html;

        server_name ec2-13-40-199-52.eu-west-2.compute.amazonaws.com;

        location / {
                proxy_pass http:://localhost:3001
        }       
}
